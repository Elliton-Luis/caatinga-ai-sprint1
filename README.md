# Caatinga.AI (Sprint 1) — rascunho

Agente que percorre pomar 12x12 inspecionando pragas (buscas, Bayes, regras).

```bash
pip install -r requirements.txt
python src/main.py 24114056
```

Saída em `resultados/` (`resultados.csv`, `grafico.png`, `pomar.txt`).

> README final (identificação, tabelas, ordem de vizinhos, limitações) em elaboração.
